#include <stdio.h>
#include "ds.cslink.h"

// 单链表反转
int main()
{
    cslink *head = cslink_init("abcdefg");

    // cslink_delete(head, 3);
    // cslink_print(head);
    // printf("\n");

    // cslink *tail = cslink_get(head, 3);
    // cslink_remove(head, tail);
    // cslink_print(head);
}
